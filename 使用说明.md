# Anything 聊天接口项目使用说明

这是一个可以部署到 Anything 的聊天接口项目。部署成功后，它会提供一个网页测试台和一个后端接口：

```text
POST /api/chat
```

接口会把用户输入发送到 Anything 项目内的 AI 模型集成，默认模型是：

```text
anthropic-claude-opus-4-7
```

也就是页面上显示的 Claude Opus 4.7。

## 这个项目能做什么

- 提供一个网页测试台
- 提供一个带 token 鉴权的 `/api/chat` 接口
- 接收 `{ "message": "你的问题" }`
- 返回 `{ "reply": "模型回复" }`
- 默认调用 Anything 内部的 Claude Opus 4.7 集成

## 这个项目目前不能做什么

- 不是 Claude Code 接口
- 不是 OpenAI 兼容接口
- 不能直接给 Cherry Studio 当模型源
- 没有联网搜索
- 没有上传文件
- 没有多轮对话记忆
- 没有流式输出

如果要给 Cherry Studio / ChatBox 使用，需要额外新增：

```text
GET /v1/models
POST /v1/chat/completions
```

## 部署前需要配置什么

请根据下面这个文件填写配置：

```text
apps/web/.env.example
```

你需要设置这些变量：

```env
API_AUTH_TOKEN=change-this-token
ANYTHING_PROJECT_TOKEN=your_anything_project_token_here
NEXT_PUBLIC_CREATE_BASE_URL=https://www.anything.com
ANYTHING_MODEL_ID=anthropic-claude-opus-4-7
```

### API_AUTH_TOKEN 是什么

这是你自己给 `/api/chat` 设置的访问密码。

别人调用你的接口时，需要带上：

```text
Authorization: Bearer 你设置的API_AUTH_TOKEN
```

例如你设置：

```env
API_AUTH_TOKEN=my-secret-token
```

调用时就要带：

```text
Authorization: Bearer my-secret-token
```

### ANYTHING_PROJECT_TOKEN 是什么

这是 Anything 项目运行时调用内部 AI 集成用的 token。

不要公开分享真实值。

如果你在 Anything 里面部署，平台可能会自动提供这个变量；如果没有，就需要在项目 Secrets / Environment Variables 里添加。

### NEXT_PUBLIC_CREATE_BASE_URL 是什么

这是 Anything 的基础地址。通常保持：

```env
NEXT_PUBLIC_CREATE_BASE_URL=https://www.anything.com
```

如果你的 Anything 环境给了其它地址，请填它提供的地址。

### ANYTHING_MODEL_ID 是什么

这是调用的 Anything 模型集成 ID。默认：

```env
ANYTHING_MODEL_ID=anthropic-claude-opus-4-7
```

如果你的账号没有这个模型权限，需要改成你账号可用的模型集成 ID。

## 如何调用接口

发布后，你的接口地址类似：

```text
https://你的项目域名/api/chat
```

请求方式：

```text
POST
```

请求头：

```text
Authorization: Bearer 你的API_AUTH_TOKEN
Content-Type: application/json
```

请求体：

```json
{
  "message": "你好，请介绍一下自己"
}
```

返回示例：

```json
{
  "reply": "你好，我是一个由 AI 模型驱动的聊天助手。"
}
```

## PowerShell 测试示例

Windows PowerShell 可以这样测试：

```powershell
Invoke-RestMethod `
  -Method Post `
  -Uri "https://你的项目域名/api/chat" `
  -Headers @{ Authorization = "Bearer 你的API_AUTH_TOKEN" } `
  -ContentType "application/json" `
  -Body '{"message":"你好，请介绍一下自己"}'
```

## curl 测试示例

```bash
curl -X POST "https://你的项目域名/api/chat" \
  -H "Authorization: Bearer 你的API_AUTH_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"message":"你好，请介绍一下自己"}'
```

## 重要安全提醒

不要把真实的 `.env` 文件发给别人。

不要公开这些内容：

```text
ANYTHING_PROJECT_TOKEN
真实 API key
真实后台 token
```

这个纯净版压缩包已经删除了 `.env` 文件，只保留了 `.env.example` 示例。

如果你把项目公开给别人使用，别人消耗的是你的 Anything 项目模型调用额度。建议设置强一点的 `API_AUTH_TOKEN`，并且不要在网页上公开展示真实 token。

## 常见问题

### 为什么部署后报 401

通常是调用接口时没有带正确的 Authorization token。

请确认请求头是：

```text
Authorization: Bearer 你的API_AUTH_TOKEN
```

### 为什么部署后报 500

通常是环境变量没有配置好，重点检查：

```text
ANYTHING_PROJECT_TOKEN
NEXT_PUBLIC_CREATE_BASE_URL
ANYTHING_MODEL_ID
```

### 为什么 Cherry Studio 获取不到模型

因为当前项目只有：

```text
POST /api/chat
```

Cherry Studio 需要 OpenAI 兼容接口：

```text
GET /v1/models
POST /v1/chat/completions
```

当前项目还没有实现这两个接口。

### 为什么 Claude Code 不能用

Claude Code 需要 Anthropic Messages API 兼容接口：

```text
POST /v1/messages
```

还需要支持工具调用。当前项目只是普通文本聊天接口，所以不适合直接用于 Claude Code。
