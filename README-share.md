# Shareable Anything Project

中文说明请看：`使用说明.md`

This is a cleaned shareable version of the Anything chat API project.

Sensitive files have been removed:

- `apps/web/.env`
- `apps/mobile/.env`

Use `apps/web/.env.example` as the configuration template. Do not share real tokens or API keys.
